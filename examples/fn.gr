// fn.gr
module Main

/// multiplies two numbers using a loop
let mul = (a, b) => {
  let mut total = 0
  let mut remaining = b
  while (remaining > 0) {
    total += a
    remaining -= 1
  }

  total
}
